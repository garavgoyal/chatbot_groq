# Empty = call /api on the same origin and let Vite's dev proxy forward it to
# the backend (see vite.config.js). Leave it empty to test on your phone over
# Wi-Fi; set it to a full URL only when the backend lives somewhere else.
VITE_API_BASE=

VITE_SUPABASE_URL=https://jmtjdnakykngqdtxanps.supabase.co
VITE_SUPABASE_ANON_KEY=
